# 메이플 패치노트 1.2.206

2026-09-10 공식 테스트월드 스킬 조정을 읽기 위한 비공식 웹사이트입니다.

- `/`: 48개 직업, 공통 및 콘텐츠 색인과 전체 검색
- `/compare`: 공식 공지의 전후 수치, 개별 상향/하향, 파티 효과 삭제
- `/history`: 직업·차수별 스킬 목록과 최근 네 패치의 시간순 이력
- `/sources`: 차수 근거, 이전 릴리즈 조사, 판정 기준 및 아이콘 출처

자료: https://maplestory.nexon.com/testworld/news/update/198 (2026-09-10 19:10 정정 포함)
직전 릴리즈는 1.2.205(8월 13일), 이전 전용 스킬 조정은 1.2.204(7월 16일)입니다. 수치 비교는 9월 10일 공지 자체의 이전/이후 값이며, 모든 값을 과거 릴리즈에서 독립 재확인한 자료가 아닙니다.

64개 섹션, 1,155개 항목과 727개 수치 비교를 담았습니다. 초기 원문 4,009개 본문 블록의 파싱 검증 후, 정정 공지를 다시 대조해 25개 항목을 갱신했습니다. 그랜드 피날레를 추가하고 돌진 문단에 붙어 있던 비홀더스 버프 표제를 분리했으며 기존 스킬 ID는 유지했습니다. 404개 상향, 297개 하향, 26개 확인 필요 판정은 각 수치 단위의 분류입니다. 직업 전체 딜 변화율이 아닙니다.

공식 직업 이미지 48개 및 스킬 항목 638개에 연결한 실제 아이콘을 로컬에 포함합니다. 아이콘은 공식 1~5차 가이드 기준이며 새 테스트 클라이언트 아이콘을 보장하지 않습니다. 미확인 아이콘은 중립 검 표시와 텍스트로 구분합니다. 출처 및 SHA-256은 `public/asset-sources.json`에 있습니다. 메이플스토리 이미지 권리는 NEXON에 있습니다.

실행: `npm install`, `npm run dev`
빌드: `npm run build`
타입 검사: `npx tsc --noEmit`

컴파일과 비브라우저 HTTP 응답, 데이터 및 로컬 이미지 참조를 확인했습니다. 실제 브라우저 상호작용 테스트는 수행하지 않았습니다.

## GitHub Pages

공개 사이트: https://n-ightmar-e.github.io/maple-patch-notes/
저장소: https://github.com/N-ightmar-e/maple-patch-notes

`npm run build:pages`는 같은 React 화면과 데이터를 Vite로 빌드해 `dist/github-pages/`에 아홉 개의 정적 HTML 진입점을 만듭니다. GitHub Pages의 `/maple-patch-notes/` 경로를 이미지와 링크에 적용합니다. 로그인 서버나 비밀 키가 필요하지 않습니다.

`main`에는 소스코드, `gh-pages`에는 생성된 파일을 보관합니다. Pages 게시 원본은 `gh-pages` 브랜치의 `/`이며 `.nojekyll`로 정적 파일을 그대로 제공합니다. 사이트를 갱신할 때는 소스 변경을 `main`에 커밋하고 `npm run build:pages` 후 결과를 `gh-pages`에 배포하면 됩니다. 기존 `npm run build`는 Sites 서버 빌드를 유지합니다.

GitHub Pages에서는 각 메뉴가 해당 HTML 페이지로 이동합니다. JavaScript를 켜야 직업 색인, 검색 및 비교 필터를 사용할 수 있습니다.

## 패치 위키 (2026-09-11 확장)

메이플 UI를 참고한 표현은 `app/maple-theme.css`에서 관리합니다. 창 제목줄, 노란 도감·전직 탭, 직업 일러스트 프레임과 스킬 슬롯에 적용하며 긴 본문은 기존 시스템 서체로 읽습니다. 게임을 흉내 낸 레벨·SP·능력치를 추가하지 않았습니다. 제목에는 공식 메이플스토리 Bold TTF 원본을 사용합니다. `public/assets/fonts/LICENSE.txt`에 공식 저작권 안내를 함께 배포하고, 원본 다운로드 주소·파일 해시는 `public/asset-sources.json`의 `fonts`에 기록합니다. 서브셋·포맷 변환 없이 제공된 파일을 그대로 사용합니다.

기본 주소는 위키 대문입니다. 48개 직업과 공통·직업군 10개를 부모 문서로 두고, 기존 1,176개 스킬·변경 기록을 개별 문서로 제공합니다. 이는 네 패치에서 조사한 항목의 위키이며, 메이플스토리 전체 스킬 목록이나 라이브 서버의 확정 수치 사전은 아닙니다. 출처가 없는 현재 능력치나 스킬 설명을 새로 채워 넣지 않았습니다.

- `/wiki/`: 직업 색인과 통합 검색. 명칭, 직업, 차수, 패치 본문에서 검색하며 띄어쓰기와 한글 초성 검색을 지원합니다. `?q=히어로+6차`처럼 검색 결과를 공유할 수 있습니다.
- `/wiki/job/?id=section-03`: 직업 문서. 차수별 목록, 검색, 주요 AI 해설, 수록 패치로 연결합니다.
- `/wiki/skill/?id=archive-f2b5ef36bae1`: 개별 항목 문서. 전후 수치, 작성된 AI/자동 해설, 최신순 이력, 원본·VI 연결, 차수 분류 근거와 출처를 표시합니다.
- `/wiki/patch/?id=p206`: 버전 문서. 해당 공지의 직업별 기록과 연결된 스킬 문서를 보여 주며 `&job=section-03`으로 좁힐 수 있습니다.
- `/notes/`, `/compare/`, `/history/`, `/sources/`: 기존 기능을 유지합니다. 이미 공유한 `/?job=...#section-...`, `/?q=...`, `/?tier=...` 주소도 기존 패치노트로 열립니다.

문서의 기존 archive ID를 그대로 사용합니다. 정적 HTML 템플릿은 문서 ID와 검색 조건을 쿼리에서 읽으므로 GitHub Pages에서도 문서 URL을 직접 열고 새로고침할 수 있습니다. 각 문서는 제목, 경로, 목차, 복사 가능한 주소, GitHub의 실제 수정 제안 링크를 갖습니다. 방문자가 사이트에서 공동 편집하는 기능은 없으며 수정 제안은 GitHub에서 사용자가 직접 제출합니다. API 키나 실시간 모델 호출을 추가하지 않았습니다.

문서 구조는 `app/wiki-reader.tsx`, 표현은 `app/wiki.css`, 검색과 링크 규칙은 `lib/wiki*.{ts,mjs}`에 있습니다. 실제 근거 데이터는 기존 `app/data/patch.json`, `skill-archive.json`, `analysis.json`, `icons.json`을 재사용합니다. 새 패치를 추가할 때 기존 ID를 유지하고 이력의 출처 참조와 AI 코멘트의 근거 해시를 함께 검토해야 합니다. 현재 사이트는 자료를 자동 수집하지 않습니다.

배포 빌드는 AI 원문 일치 검사, 아카이브 관계 검사, 위키 검색·주소 호환 검사를 먼저 실행합니다. `node scripts/check-wiki.mjs`는 고유 문서 주소, 부모 문서와 이미지 연결, 한글 검색, 기존 공유 주소, 9개 진입점 구성을 확인합니다. 이번 확장은 타입 검사·빌드·데이터 검사·실제 배포 HTTP 응답으로 검증하며, 브라우저 화면과 클릭 동작 검사는 수행하지 않았습니다.


## 차수별 스킬 이력 (2026-09-11 추가)

현재 패치의 직업·공통 항목 1,112개와 앞선 공지의 스킬 표제를 묶은 1,176개 기록입니다. 콘텐츠/코어 관리 항목은 기존 패치노트 화면에서 유지합니다. 네 공지의 수록 범위는 1.2.203(2026-07-09), 1.2.204(07-16), 1.2.205(08-13), 1.2.206(09-10)입니다. 전체 서비스 기간의 연혁이 아닙니다.

- 차수 필터, 스킬별 이력 링크, 패치 버전 필터, 이전 이력만 보기, 검색을 제공합니다.
- 기본, 1–4차, 하이퍼, 5차, 6차 외에 듀얼블레이드 1.5·2.5차와 제로 알파·베타·초월자를 별도로 표시합니다.
- 970개 기록은 공식 가이드/차수 공지의 표제와 대조했습니다. 21개는 공식 스킬명의 VI 표기로 6차에 분류하고 별도로 표시했습니다. 나머지는 공통 44개, 기타 7개, 차수 확인 필요 134개입니다. 구 가이드 차수가 현재 클라이언트와 일치한다는 보장은 하지 않습니다.
- 나무위키는 직접 확인하려 했으나 브라우저 보안 정책과 접근 제한으로 내용을 확인하지 못했습니다. 나무위키를 확인한 것처럼 출처를 달지 않았습니다.
- 직업과 스킬명의 정규화된 정확한 일치만 연결합니다. VI와 원본의 이력은 합치지 않고 서로 이동할 수 있게 연결했습니다. 과거 공지의 개명, 표제 없이 언급된 변경, 조사 기간 밖 기록은 빠질 수 있습니다. 이전 기록 미발견을 변경 없음으로 해석하지 않습니다.
- 이력 본문은 테스트월드 공지 기준입니다. 본 서버 적용 여부나 직업 전체 성능 변화를 뜻하지 않습니다.

분류 데이터와 이력은 `app/data/skill-archive.json`에 있습니다. 각 기록의 `classification`은 근거 URL, 문서 내 차수 제목, 분류 방법을 보관합니다. `events`의 `patchId`는 기존 패치 데이터에 연결하고, 이전 이벤트는 출처 ID와 공지 본문을 저장합니다. 같은 직업의 일반 변경 표제가 중복되어도 모든 현재 패치 항목을 보존합니다.

차수 근거: 넥슨 이전 직업별 스킬 가이드 및 공식 업데이트 공지 660(모험가 리마스터), 712(6차 도입), 735·760(마스터리 코어), 747(아란·은월), 775(렌), 788(와일드헌터), 792(키네시스), 793(솔 헤카테), 807(레테), 테스트월드 186(신규 HEXA 스킬). 3·4번째 마스터리 코어의 출시 순서는 3·4차 전직으로 취급하지 않습니다.

검증: `node scripts/check-archive.mjs`는 현재 1,112개 항목의 보존, 출처 참조, 직업 간 잘못된 연결, 주요 특수 차수와 4개 버전의 연속 기록을 검사합니다. `npx tsc --noEmit` 및 변경한 TSX/TS 파일의 `oxlint`, `npm run build:pages`로 코드와 빌드를 확인합니다. 이번 변경에서도 브라우저 상호작용 테스트는 수행하지 않았습니다.


## 사전 AI 분석 코멘트 (2026-09-11 추가)

48개 직업의 주요 변경을 포함한 55개 코멘트를 `app/data/analysis.json`에 수록했습니다. AI(Codex)가 공식 정정 공지의 변경 전후 수치와 기능 설명을 읽고 작성한 고정 분석입니다. 실시간 AI 호출, 비밀 키, 추가 API 비용, 게임 내 실측은 없습니다. 공지에 없는 종전 공식·발동 빈도·전체 피해 비중을 알고 있다고 주장하지 않습니다.

각 코멘트는 핵심 해석, 기존 운용과 달라지는 점, 추가 확인 조건을 보여 줍니다. 기존 스킬 카드와 차수별 이력 화면에서 읽을 수 있고, 직업 첫 화면에는 해당 직업의 주요 분석으로 이동하는 링크를 둡니다. 686개 항목에 생성되는 결정적 수치·조건 해설은 ‘규칙 기반’으로 구분하며 AI 심층 검토로 표시하지 않습니다.

조건부 계산에는 단위·대상·충전 방식·적중 상한이 바뀌는 예외를 명시합니다. A.D 오드넌스는 응집 최대 횟수 모두 적중 및 구체 1회라는 조건에서 표기계수합과 쿨타임 정규화 비율을 따로 제시합니다. DPS 또는 캐릭터 전체 성능의 추정치가 아닙니다.

`node scripts/check-analysis.mjs`는 모든 코멘트의 원문 SHA-256 일치, 48개 직업 수록, 공식 정정 반영, 단위 혼용/충전 방식의 비교 제외, 조건부 산술을 검사합니다. `npm run build:pages`가 이 검사를 먼저 실행하므로 원문을 바꾼 뒤 과거 코멘트를 그대로 게시하는 것을 막습니다. 원문이 바뀌면 해석을 재검토한 뒤 해당 코멘트의 근거 해시도 갱신해야 합니다.
